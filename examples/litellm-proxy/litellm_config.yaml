# Generated by subagent-fleet generate --config examples/fleet.yaml
# Source: examples/fleet.yaml

model_list:
   - model_name: claude-sonnet-local
     litellm_params:
       model: ollama_chat/qwen2.5-coder:32b
       api_base: http://192.168.1.50:11434
       api_key: ollama
       timeout: 600
     model_info:
       max_input_tokens: 32768

   - model_name: claude-haiku-local
     litellm_params:
       model: ollama_chat/qwen2.5-coder:7b
       api_base: http://192.168.1.51:11434
       api_key: ollama
       timeout: 300
     model_info:
       max_input_tokens: 8192

   - model_name: math-planner-local
     litellm_params:
       model: ollama_chat/dolphin-math:8b
       api_base: http://192.168.1.50:11434
       api_key: ollama
       timeout: 450
     model_info:
       max_input_tokens: 16384

   - model_name: llama-gpu-local
     litellm_params:
       model: ollama_chat/llama3.1:70b
       api_base: http://192.168.1.100:11434
       api_key: ollama
       timeout: 900
     model_info:
       max_input_tokens: 131072

litellm_settings:
   drop_params: true
   master_key: os.environ/LITELLM_MASTER_KEY

router_settings:
   routing_strategy: simple-shuffle
   num_retries: 1
   timeout: 600

# This config routes requests to the right Ollama model and node.
# Each `model_name` is a LiteLLM alias that subagent-fleet agents reference.
# The `api_base` points to the machine running Ollama with that model loaded.
#
# To use this file:
#   litellm --config ./litellm_config.yaml --host 127.0.0.1 --port 4000
#
# Or point Claude Code at it:
#   export OPENAI_API_BASE=http://localhost:4000
#   claude
